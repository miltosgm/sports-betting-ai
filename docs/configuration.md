# ⚙️ Configuration

How to configure the model and data pipelines.

**Status:** 📋 COMING SOON

---

## Config Files

Configuration is in `config/` directory:

```
config/
├── data_config.yaml       (Data sources & collection)
├── feature_config.yaml    (Feature definitions)
├── model_config.yaml      (Model hyperparameters)
└── betting_config.yaml    (Bet sizing & risk rules)
```

---

## Data Configuration

Configure data sources in `config/data_config.yaml`

---

## Model Configuration

Tune model hyperparameters in `config/model_config.yaml`

---

## Betting Configuration

Set risk parameters in `config/betting_config.yaml`

---

Full documentation coming soon.
